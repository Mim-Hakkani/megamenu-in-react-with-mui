import React from 'react';
import CustomizedMenu from './CustomizedMenu';
// import CustomizedMenu from './demo';

const SuperMenu = (props) => (
  <div style={{}}>
    <CustomizedMenu />
    <CustomizedMenu />
    <CustomizedMenu />
  </div>
)

export default SuperMenu;